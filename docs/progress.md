# UX Overhaul — Progress Log

See `ROADMAP.md` → _Mid-term (UX Overhaul)_ for the full spec of each item.

---

| #   | Item                        | Branch                               | Status    |
| --- | --------------------------- | ------------------------------------ | --------- |
| 1   | WYSIWYG canvas              | `feature/ux-wysiwyg-canvas`          | ✅ merged |
| 2   | Select-to-style             | `feature/ux-select-to-style`         | ✅ merged |
| 3   | Inline text editing         | `feature/ux-inline-text-editing`     | ✅ merged |
| 4   | Visual property editors     | `feature/ux-visual-property-editors` | ✅ merged |
| 5   | Layers / outline panel      | `feature/ux-layers-panel`            | ✅ merged |
| 6   | Context menu                | `feature/ux-context-menu`            | ✅ merged |
| 7   | Pre-built component library | `feature/ux-component-library`       | ✅ merged |
| 8   | Responsive preview          | `feature/ux-responsive-preview`      | ✅ merged |
| 9   | Keyboard shortcuts          | `feature/ux-keyboard-shortcuts`      | ✅ merged |
| 10  | Canvas helpers              | `feature/ux-canvas-helpers`          | ✅ merged |
| 11  | Empty-state guidance        | `feature/ux-empty-state`             | ✅ merged |

---

## Refactoring

- **2026-07-28** Consolidate duplicate `buildComponentTree` — `chore/consolidate-buildComponentTree` merged. Deleted redundant `buildComponentTree()` from `canvas.js`, imports `buildComponentTemplate()` from `components.js`. Removed ~40 lines of duplicate code.
- **2026-07-28** `cssState.js` module + migration — `refactor/css-state-migration` merged. Created `src/js/config/cssState.js` to centralize `activeCssData` state management. Replaced all `window.activeCssData` direct access in `app.js`, `inspector.js`, and `storage.js` with cssState API. Kept backward-compat reference. Eliminated ~68 global state references.
- **2026-07-28** Fix preview mode CSS aggression — `fix/preview-mode-css` merged. Replaced aggressive `* { border/outline/background: none !important }` with targeted selectors that only strip editor chrome. User styles now render faithfully in preview.
- **2026-07-28** Add exporter unit tests — extracted `buildExportHtml`, `buildExportCss`, `cleanStyles` from DOM closure into exported pure functions. 5 tests cover HTML template, CSS concatenation, and recursive style cleanup.
- **2026-07-28** Fix layer highlighting uniqueness — replaced fragile `textContent.includes()` match in `highlightLayerEntry` with `data-node-id` attribute (auto-assigned by `getNodeId()`). Selection now pinpoints exactly one entry instead of all matching tag names.
- **2026-07-28** All 6 short-term refactoring items complete. ActiveCssData migrated to cssState.js, preview CSS fixed, tests added, auto-save interval reviewed, layer highlighting uniqueness fixed.
- **2026-07-28** Mid-term (Feature) #2: Advanced CSS blocks — `feature/advanced-css-blocks` merged. cssState.js changed to tree data model (blocks array with type/selector/styles/children). Recursive compileAndRenderCss emits `@media`/`@keyframes` wrappers. New +Add Media / +Add Keyframes buttons create container boxes in the CSS editor with +Add Nested Rule support. CSS dictionary extended (Animation & Transforms, Custom Properties). Property edits on nested rules use tree-aware API. 11 new tests for tree API, nested properties, and old-format upgrade.

## Recent work

_(appended automatically after each branch merge)_

- **2026-07-28** UX #1 WYSIWYG canvas — `feature/ux-wysiwyg-canvas` merged. Removed wireframe dashed borders from default edit mode. Added hover highlight (`.el-hover`) on canvas children. Added "🔲 Outlines" toggle button in control bar to restore wireframe view on demand.
- **2026-07-28** UX #2 Select-to-style — `feature/ux-select-to-style` merged. Added inline "Styles" section in inspector panel. When user edits a CSS property, system auto-generates a unique class (`._lc-N`), creates the CSS rule in `activeCssData`, and compiles live styles. Undo support included.
- **2026-07-28** UX #3 Inline text editing — `feature/ux-inline-text-editing` merged. Double-click any text element (p, h1-h6, span, a, button, label, li, th, td) on canvas to edit directly. Enter commits, Escape cancels, blur commits. Green outline indicates editing state. Undo support included.
- **2026-07-28** UX #4 Visual property editors — `feature/ux-visual-property-editors` merged. Replaced plain text inputs in the inline style editor with visual widgets: color swatch + native picker (color, background-color), dropdown (font-weight), alignment button group (text-align), and font-size presets (XS–2XL) with custom text fallback.
- **2026-07-28** UX #5 Layers / outline panel — `feature/ux-layers-panel` merged. Added a layers tree view in the right sidebar showing all canvas elements with indentation hierarchy. Click to select, eye icon to toggle visibility, lock icon to prevent interaction. Auto-refreshes on DOM changes; rehydrates on project load.
- **2026-07-28** UX #6 Context menu — `feature/ux-context-menu` merged. Right-click any canvas element opens a context menu with: Edit Text (text elements), Duplicate, Delete, Move Up/Down, Copy/Paste Styles, Wrap in &lt;div&gt;. All actions push undo history.
- **2026-07-28** UX #7 Pre-built component library — `feature/ux-component-library` merged. Added a "Components" category in the left toolbox with 9 pre-built blocks (Hero Section, Feature Grid, Navbar, Card, Contact Form, Footer, Testimonial, Pricing Table, FAQ). Each expands into a full DOM tree with inline styles and placeholder content when dropped. Undo/redo support.
- **2026-07-28** UX #8 Responsive preview — `feature/ux-responsive-preview` merged. Preview mode now includes a dark toolbar with device frame buttons: Desktop (full width), Tablet (768px), Mobile (375px). Canvas viewport resizes to match selected device, centered. Live pixel width indicator shown.
- **2026-07-28** UX #9 Keyboard shortcuts — `feature/ux-keyboard-shortcuts` merged. Extended existing Ctrl+Z/Ctrl+Shift+Z with: Ctrl+C copy, Ctrl+V paste, Delete/Backspace delete, Ctrl+A select first element, Escape deselect, arrow keys nudge by 1px.
- **2026-07-28** UX #10 Canvas helpers — `feature/ux-canvas-helpers` merged. Added 16px snap grid overlay toggle (button in control bar). Draggable red guide lines: double-click canvas to place, drag to move, right-click to remove.
- **2026-07-28** UX #11 Empty-state guidance — `feature/ux-empty-state` merged. Replaced simple placeholder text with a styled card (icon, title, subtitle, keyboard hints). Quick Start button drops Navbar + Hero. Load Sample button creates a full sample page.
- **2026-07-31** Complete `src/js/config/` — `chore/complete-config` merged. Extracted remaining pure data from modules into config: new `styleProps.js` (inspector style editor grids), `devices.js` (breakpoint presets with SVG icon names), `codegen.js` (JSX attr map / boolean attrs / void elements); `elements.js` gained `TAG_LABELS` (layers tree labels). Updated `inspector.js`, `exporter.js` (device buttons now render via `<span class="mat-icon">`), `layers.js`, `codegen/domWalker.js`, and `app.js` (breakpoint validation against `DEVICES`) to import from config. README + AGENTS.md module boundaries updated. 125 tests pass.
- **2026-07-31** Per-rule CSS collapse — `feat/css-rule-collapse` merged. Each CSS rule box (plain rule, media/keyframes container, nested rule) now has a chevron toggle in its header to collapse/expand that item individually (panel-wide collapse untouched). Implemented via event delegation in `initRuleBoxCollapse()`; chevron rotates -90° when collapsed and hidden parts (body dropzone, `+ Add Nested Rule`, closing brace) are suppressed via `.css-rule-box.collapsed`. New i18n strings (`ui.detection.collapse/expand`) in en/zh-TW/ja. +1 test.
- **2026-07-31** Account-free positioning — product decided as open-source / self-hosted / local-first tool (docs/ROADMAP.md "Product Positioning" + new P4 plan). Auth UI commented out everywhere: toolbar `#btn-auth` (already), `#auth-modal`, File menu "Pull from cloud" button, and the `initAuthUI`/`updateAuthButton`/`showAuthModal`/`syncOnStart`/`showConflictModal` functions in app.js (with un-comment instructions inside). App imports trimmed; bundle -3.4 kB. Sync module (`sync.js`) and its tests untouched so self-hosters can re-enable cloud sync later. 126 tests pass.
- **2026-07-31** File-first sharing (P4b) — `feature/file-sharing` merged. New `src/js/modules/projectFile.js`: `.lcproj` JSON export (`Export Project` menu item, file named after project) and import (menu item + drag-and-drop the file anywhere on the window; auto-renames on collision, rehydrates canvas/CSS/layers/history, adds to project list). Exporter gained "Single-file HTML (inlined CSS)" option. Template gallery now also loads drop-in templates from `public/templates/manifest.json` at runtime (`my-hero.json` example + README included); missing manifest falls back to built-ins silently. i18n strings for import success/invalid in en/zh-TW/ja. +12 tests (138 total).
- **2026-07-31** Template library expansion (P4c) — `feature/template-library` merged. 5 → 22 built-in templates: split hero, feature grid, testimonials, CTA banner, stats, team, blog card list, FAQ, dashboard shell, profile card, chat list, todo list, signup/contact/newsletter forms, product grid, cart summary. Gallery icon map extended for all 22. Data-structure test updated (22) — `it.each` now validates every template's required fields automatically. 155 tests pass.
- **2026-07-31** P4a self-host quickstart — `docs/self-hosting.md` (requirements, local install, production build, deploy options incl. nginx config, Docker usage with build-arg Supabase env, hosted-vs-self-hosted Supabase, honest collab-transport status), multi-stage `Dockerfile` (node:20 → nginx:1.27), `docker-compose.yml` (one command, `LC_HTTP_PORT` override), `.dockerignore`, README Quick Start / Self-hosting sections + project-structure update.
- **2026-07-31** Design tokens (P4d) — `feature/design-tokens` merged. New `src/js/modules/tokenEditor.js`: "Design Tokens" section in the Visual CSS Rules sidebar (add via prompt, rename, live value editing, delete). `cssState.js` gained `getTokens/setTokens/setToken/deleteToken` (names must start with `--`; cleared by `initCssState`). Tokens persist per-project: `storage.js` save/load, `.lcproj` export/import (version 2, old files still validate), rehydrated via `window.rebuildTokenUI` on load/import. `compileAndRenderCss()` emits a `:root { … }` block first, so preview/export/single-file HTML all include tokens. Fixed latent bug: `window.saveProject` was never assigned (drop-in template saves silently no-op'd). i18n strings (`ui.tokens.*`) in en/zh-TW/ja. vitest `hookTimeout`/`testTimeout` 30s (parallel-worker margin). 173 tests pass.
- **2026-07-31** Token-aware design system UI (P5b) — `feature/token-aware-inspector` merged. New `src/js/modules/tokenPicker.js`: palette button next to every CSS block value input and Inspector style row opens a popup listing current tokens (click inserts `var(--name)`) with a "Save as token" action (prompts a name, stores the current value, applies `var()` immediately; token panel refreshes via `window.rebuildTokenUI`). `cssState.replaceTokenRef(old, new)` rewrites `var(old)` → `var(new)` across all rules incl. media-query children, wired into the token panel rename handler. Templates `navbar`, `dashboard-layout`, `signup-form` carry a `tokens` seed field; `instantiateTemplate` seeds tokens on replace and merges on append (drop-in templates get this for free). 185 tests pass.
